import { PlaceholderTab } from "./PlaceholderTab";

export function OpenEndedTab() {
  return (
    <PlaceholderTab
      id="open"
      buildStep={7}
      talkingPoint="Maximum freedom. Slower, less predictable, more expensive — the experimental frontier. Watch what happens when I flip to live."
    />
  );
}
