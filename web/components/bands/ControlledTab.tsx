import { PlaceholderTab } from "./PlaceholderTab";

export function ControlledTab() {
  return (
    <PlaceholderTab
      id="controlled"
      buildStep={4}
      talkingPoint="The agent chose which component to render and filled the data. I guaranteed how it looks. Pixel-perfect every time."
    />
  );
}
