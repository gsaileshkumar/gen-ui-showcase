import { PlaceholderTab } from "./PlaceholderTab";

export function DeclarativeTab() {
  return (
    <PlaceholderTab
      id="declarative"
      buildStep={5}
      talkingPoint="I shipped the lego bricks — Card, Stat, List, PriceTable. The agent built the layout. This is where the long tail lives."
    />
  );
}
